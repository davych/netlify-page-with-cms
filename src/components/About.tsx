import React from 'react';
import { Link } from 'gatsby';

import type { About as AboutProps } from '../types/app';
import { fieldPath } from '../utils/annotations';

export const About = (props: AboutProps) => {
    return (
        <div>hi</div>
    );
};
